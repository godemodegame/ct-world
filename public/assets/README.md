Put your game assets here:

- `map.png` - the 2D top-down map texture.
- `hero.fbx` - the 3D character model. `hero.glb` and `hero.gltf` also work.

The prototype will run with placeholders until these files are added.
